; Limites de upload (PHP lê este ficheiro em muitos servidores com PHP-FPM/CGI).
; Se não surtir efeito, defina os mesmos valores no php.ini do servidor ou no pool FPM.
upload_max_filesize = 32M
post_max_size = 40M
max_file_uploads = 20
